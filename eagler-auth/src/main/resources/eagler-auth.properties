enabled=true
# URL of the auth endpoint which accepts POST JSON {"username":"...","address":"..."}
# It must return HTTP 200 with a JSON body containing "authorized":true when allowed.
auth_url=http://localhost:8080/auth
kick_message=You are not authorized to join this server.
